pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'git@github.com:A-Yerkeshev/sw-project-2024-fahr-to-cel.git'
            }
        }
    }
}